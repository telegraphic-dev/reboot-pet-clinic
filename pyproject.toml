[project]
name = "petclinic-reboot"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["reboot==1.6.0"]

[dependency-groups]
dev = [
  "reboot[dev]==1.6.0",
  "mypy==1.18.1",
  "pytest>=7.4.2",
  "types-protobuf>=4.24.0.20240129",
]
